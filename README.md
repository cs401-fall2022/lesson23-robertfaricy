To run site, in bash from file directory containing
-bin
-public
    |
    -css
-routes
-views
app.js
*.json

Use the commands:

$ npm install
$ npm i -g npm-check-updates
$ DEBUG=myapp:* npm start

Site is located at:
http://localhost:3000/
